{
  "name": "base44-plantas-monorepo",
  "version": "2.0.0",
  "description": "Monorepo HTML para gestión de plantas Base44",
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev:simple",
    "dev:simple": "npx serve apps/plant-manager -p 8080",
    "dev:manager": "cd apps/plant-manager && python3 -m http.server 8080",
    "build": "echo 'No build step required for static HTML'",
    "test": "echo 'No tests configured yet'",
    "lint": "echo 'Linting not configured'",
    "clean": "rm -rf node_modules packages/*/node_modules apps/*/node_modules"
  },
  "keywords": [
    "plants",
    "inventory",
    "base44",
    "monorepo",
    "html",
    "vanilla-js"
  ],
  "author": "Base44",
  "license": "MIT",
  "devDependencies": {
    "serve": "^14.2.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
